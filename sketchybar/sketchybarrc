source "/Users/psg/.config/scripts/palette.sh"

PLUGIN_DIR="$CONFIG_DIR/plugins"
ITEM_DIR="$CONFIG_DIR/items"

sketchybar --bar position=top height=38 blur_radius=0 color="$(hex_to_rgba $bg_dim)"

default=(
  padding_left=5
  padding_right=5
  icon.font="Monaco Nerd Font:Bold:14.0"
  label.font="Monaco Nerd Font:Regular:14.0"
  icon.color="$(hex_to_rgba $fg)"
  label.color="$(hex_to_rgba $fg)"
  icon.padding_left=4
  icon.padding_right=4
  label.padding_left=4
  label.padding_right=4
  updates=on
)
sketchybar --default "${default[@]}"

source $ITEM_DIR/aerospace.sh

source $ITEM_DIR/front_app.sh

source $ITEM_DIR/clock.sh
source $ITEM_DIR/volume.sh
source $ITEM_DIR/battery.sh
source $ITEM_DIR/wifi.sh

sketchybar --update
